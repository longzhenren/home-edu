server:
  port: 8703
spring:
  application:
    name: home-class
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos
        namespace: public
      config:
        group: DEFAULT_GROUP
        server-addr: 127.0.0.1:8848
  config:
    import:
      - optional:nacos:home-class.yml
grpc:
  server:
    port: 9703
