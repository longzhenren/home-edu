server:
  port: 8002
spring:
  application:
    name: home-auth
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.2.154:8848
        username: nacos
        password: nacos
        namespace: public
      config:
        group: DEFAULT_GROUP
        server-addr: 192.168.2.154:8848
  config:
    import:
      - optional:nacos:home-auth.yml
